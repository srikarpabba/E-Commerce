export interface User {
  email: string;
  displayName: string;
  phoneNumber: string;
  token: string;
}

export interface Address {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  addressLine: string;
  city: string;
  state: string;
  zipCode: string;
}